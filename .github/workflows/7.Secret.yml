name: 7.Secrets
on:
  push:
    branches:
      - main
      - develop
      - 'release/**'
  pull_request:
    types:
      - opened
    branches:
       - main
 
jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - name: Build Step
              run: echo "Hello , ${{vars.MY_NAME}}"
              run: echo "Hello , ${{secrets.DOCKER_PASSWORD}}"
